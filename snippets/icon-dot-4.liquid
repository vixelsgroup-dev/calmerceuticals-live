{%- liquid
  assign size = size | default: 4
-%}
<svg
  xmlns="http://www.w3.org/2000/svg"
  width="{{ size }}"
  height="{{ size }}"
  viewBox="0 0 4 4"
  fill="none"
  aria-hidden="true"
  focusable="false"
>
  <circle cx="2" cy="2" r="2" fill="currentColor"/>
</svg>
